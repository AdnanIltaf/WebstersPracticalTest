@model IEnumerable<WebstersPracticalTest.Models.Fine>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FineName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FineCharges)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FineName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FineCharges)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Fineid }) |
            @Html.ActionLink("Details", "Details", new { id=item.Fineid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Fineid })
        </td>
    </tr>
}

</table>
